<h1>Products:<h1>

<% @products.each do |product| %>
  <div>
    <%= link_to product_path(product) do %>
      <h2><%= product.title %></h2>
      <p><%= product.description %></p>
      <p><%= product.price %></p>
    <% end %>
  </div>
<% end %>


